{
  "name": "seller-feature-dashboard",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/seller/feature-dashboard/src",
  "prefix": "seller",
  "projectType": "library",
  "tags": ["scope:seller", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/seller/feature-dashboard"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
