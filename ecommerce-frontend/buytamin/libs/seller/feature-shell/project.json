{
  "name": "seller-feature-shell",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/seller/feature-shell/src",
  "prefix": "seller",
  "projectType": "library",
  "tags": ["scope:seller", "type:feature"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/seller/feature-shell"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
